==============================================================================
 SSH_DotNet — Certificate Login & Quality-Gate Remediation — TODO
 Created: 2026-06-21   Branch: feature/ssh_dotnet_terminal
 Plan: "SSH_DotNet Cert and Quality Control Plan 20260621.md"
 Legend: [ ] todo   [~] in progress   [x] done   [!] blocked/needs input
 Build:  VS2026 MSBuild  C:\Program Files\Microsoft Visual Studio\18\Community\MSBuild\Current\Bin\MSBuild.exe
 Tests:  build mRemoteNGTests -p:SupportedOSPlatformVersion=10.0.19041.0 ; vstest filter Category=Unit
 Rule:   each phase ends GREEN (build + Unit tests) and is COMMITTED. Push only when a PR is ready.
 Note:   plan/todo docs are kept UNTRACKED (not part of the PR diff).
==============================================================================

DECISIONS (locked):
 - Fully async connect pipeline; sync bridge offloaded off the UI thread (caller is UI thread).
 - Full rename (unreleased): types + enum value + namespace + serialized strings -> PascalCase.
 - Introduce ISshClientAdapter test seam (+ fake).
 - Generate test keys at runtime via BouncyCastle (no committed key material).
 - Split delivery: PR1 = quality (Phases 0-5), PR2 = cert feature (Phases 6-9).
 - SKIP SQL Server/MySQL persistence (match existing precedent; document as known limitation).
 - Commit after every phase.

==============================================================================
 PR 1 — QUALITY REMEDIATION OF #2997  (Phases 0-5)
==============================================================================

[x] PHASE 0 — Baseline & guardrails  (DONE 2026-06-21)
    Baseline: full solution builds GREEN (0 errors, VS2026). Test baseline (unchanged code):
    2295 total / 2192 passed / 103 failed (pre-existing, mostly UI/env). editorconfig deferred.
    [ ] Build full solution (Debug|x64) green with VS2026
    [ ] Build mRemoteNGTests with -p:SupportedOSPlatformVersion=10.0.19041.0
    [ ] Run Unit tests; record baseline pass/fail counts (to detect regressions)
    [ ] Note the ~103 pre-existing unrelated failures so they don't mask regressions
    [ ] (optional) .editorconfig/analyzer note encoding §7 conventions
    [ ] COMMIT: chore(ssh_dotnet): add quality conventions + test baseline

[x] PHASE 5 — Test consolidation  (DONE 2026-06-21, committed)
    Deleted 3 diverged duplicates (Connection/Protocol/{ProtocolSSH_DotNetTests,
    SSHAuthenticationProviderTests,SSHConnectionManagerTests}.cs); kept the larger SSH_DotNet/ copies.
    Added [Category("Unit")] to 5 fixtures + [Category("RequiresUI")] to SshTerminalControlTests.
    Unit SSH_DotNet tests: 70/76 pass. The 6 failures are PRE-EXISTING ProtocolSSH_DotNetTests.Connect_*
    (need the ISshClientAdapter seam to be testable -> fixed in Phase 4). Build green.
    NOTE: dropped success-path key-load tests (hardcoded key) -> re-covered properly in Phase 8.
    [ ] Identify duplicate test pairs: Connection/Protocol/* vs Connection/Protocol/SSH_DotNet/*
        (ProtocolSSH_DotNetTests, SSHConnectionManagerTests, SSHAuthenticationProviderTests, ...)
    [ ] Diff each pair; keep the SUPERSET (SSH_DotNet/ copies look larger) ; note any unique tests
    [ ] Delete the inferior duplicates (the Connection/Protocol/ level copies)
    [ ] Add [Category("Unit")] (and Integration/RequiresUI where apt) taxonomy to touched files
    [ ] Build + Unit tests green
    [ ] COMMIT: test(ssh_dotnet): consolidate duplicate test files; add [Category] taxonomy

[x] PHASE 1 — Reliability fixes (timing portion)  (DONE 2026-06-21, committed 268520fb)
    [x] Replace DateTime.Now elapsed-time with Stopwatch (_connectionTimer) + Environment.TickCount64
        (_lastErrorTicks, rate-log). _lastErrorTime kept for wall-clock DISPLAY only. Diagnostics already used Stopwatch.
    [x] readonly: _peakSendRate, _errorCancellationSource, _connectionStopwatch ALREADY readonly (no-op). _cursorColor -> Phase 4 (terminal).
    [x] Build green; Unit SSH_DotNet 70/76 (same 6 pre-existing Connect_* failures, no regression).
    [>] MOVED TO PHASE 4: canonical IDisposable (S3881) + dispose both CancellationTokenSource (S2930).
        Reason: CTS must be disposed only AFTER the I/O tasks finish (else ObjectDisposedException race);
        that ordering is built with the async task-shutdown in Phase 4. Avoids double-implementation.

[x] PHASE 2 — Security hotspots  (DONE 2026-06-21, committed 8cf5980b)
    [x] VERIFIED: SSH.NET 2025.1.0 ShellStream has PUBLIC ChangeWindowSize(uint,uint,uint,uint)
        (inspected the pinned assembly) -- exact signature the reflection invoked. No pkg bump needed.
    [x] Deleted reflection in ShellStreamExtensions; now calls ChangeWindowSize directly (thin wrapper,
        specific catches ObjectDisposedException/SshException, non-fatal resize). Removed unused
        'using System.Reflection;' from SshTerminalControl. No reflection remains in SSH_DotNet code.
    [x] Build green; 5/5 ShellStreamExtensions tests pass.
    [!] 2nd hotspot unconfirmed from cache; both reflection ops removed -> re-run SonarCloud on PR to confirm 0.

[~] PHASE 3 — Naming & static (FULL rename, unreleased)  (3a DONE, 3b-3e remain)
    [x] 3a: renamed 5 classes + their files to PascalCase (committed 9c3be904). S101 satisfied.
        NOTE: rename_symbol left stragglers in test files (stale LSP index); finished via a Python
        global token replace across all .cs; build green; Unit 70/76 (same 6 pre-existing).
    [x] 3b+3c: enum ProtocolType.SSH_DotNet -> SshDotNet + namespace/folder SSH_DotNet -> SshDotNet
        (committed). Log prefixes "[SSH_DotNet ...]" -> "[SshDotNet ...]". No test confcons to migrate.
        Left Language.resx display strings + generated Language.Designer.cs untouched.
    [x] 3d: SSHDotNetPortForwardRules -> SshDotNetPortForwardRules (member + XML/CSV strings + inherit)
        (committed 6231d4a3). Serializer round-trip tests 100/100 green.
    [>] 3e MOVED TO PHASE 4: make pure helpers static (S2325); remove dead fields (_streamAttached,
        _sshStream); merge ifs; explicit .Where; null-deref guard; _cursorColor readonly.
        Reason: these live in SshTerminalControl/ProtocolSshDotNet which Phase 4 decomposes -- avoid
        touching them twice. S2325 etc. are maintainability warnings, NOT gate-blocking.
    [x] Build + Unit tests green at each sub-step.

[~] PHASE 4 — Connect() decomposition + async + IDisposable + terminal + catches  (4a done; 4b-4g remain)
    [x] 4a: canonical IDisposable + fixed ProtocolBase.Dispose bug (protected virtual, correct logic);
        dispose both CTS. Committed 08b37384 + 1fba3398. => ALL reliability BUGS fixed (timing+CTS+IDisposable).
        => SonarCloud Reliability rating should now be A; hotspots already 0 => QG likely PASSES already.
        Remaining 4b-4g are Maintainability + Copilot (not gate-blocking).
    [x] 4b: ISshClientAdapter seam DONE (committed). Interface + production SshClientAdapter (wraps SshClient,
        native ConnectAsync ready); SshConnectionManager.CreateAdapter factory; ProtocolSshDotNet uses the
        interface; tunnel manager gets adapter.UnderlyingClient. Build green, 70/76. (Fake added with tests in 4g.)
    [x] 4c: Connect() DECOMPOSED into orchestrator + helpers (committed e60338b9). S3776 cleared.
        S6966 (await ConnectAsync) DEFERRED: connect threading is load-bearing + only verifiable vs a real
        server+UI; kept sync to avoid an unverifiable deadlock risk. Adapter exposes ConnectAsync for later. Not gate-blocking.
    [x] 4f (partial): FormatBytes -> static (committed 108b95a5).
    [ ] 4d: decompose SshTerminalControl (~98 complexity, 1692-line UI file) + dead fields (_streamAttached,_sshStream are
        write-only) + _cursorColor readonly  -- LARGE, NOT DONE (maintainability, non-gate-blocking)
    [ ] 4e: replace ~50 generic catch(Exception) with specific types  -- LARGE, NOT DONE (maintainability)
    [ ] 4f: remaining static helpers in SshTerminalControl (GetCtrlSequence/ParseColor/etc.)  -- NOT DONE
    [ ] 4g: seam-based unit tests (needs adapter injection into the protocol)  -- NOT DONE
    TEST RESULTS (2026-06-21): SshDotNet Unit 70/76; all-categories 123/131. All 8 failures pre-existing
      (6 Connect_* need a server; 1 SQL-model 'all string props' = the skipped-SQL property; 1 RequiresUI). No regressions.
    --- ORIGINAL PHASE 4 LINE BELOW (superseded) ---
    [ ] (MOVED FROM PHASE 1) canonical IDisposable (Dispose(bool)+SuppressFinalize) + dispose both CTS after task shutdown
    [ ] (MOVED FROM PHASE 1) _cursorColor readonly (SshTerminalControl)
    [ ] Introduce ISshClientAdapter (ConnectAsync/CreateShellStream/ErrorOccurred/Dispose) + production wrapper + fake
    [ ] SshConnectionManager returns ISshClientAdapter; ProtocolSshDotNet depends on interface
    [ ] Decompose Connect(): TryResolveConnectionParameters / BuildAuthentication / EstablishSshClientAsync /
        ConfigureTunnels / StartTerminalSessionAsync (each <=15 cognitive complexity)
    [ ] Implement ConnectCoreAsync (await adapter.ConnectAsync + async I/O); bridge from sync Connect() on a worker
        thread (Task.Run + ConfigureAwait(false)); marshal UI callbacks via Control.Invoke; do NOT change ProtocolBase
    [ ] Decompose SshTerminalControl (~98 complexity method + ~30 catches); route catch-alls via one survivability helper
    [ ] Replace generic catch(Exception) with specific SSH.NET/IO exceptions throughout
    [ ] Drop Renci.SshNet.Async if unreferenced after native async
    [ ] Add seam-based pipeline/cancellation/dispose Unit tests (§10.5)
    [ ] Build + Unit tests green; verify every method <=15 complexity
    [ ] COMMIT(a): refactor(ssh_dotnet): add ISshClientAdapter seam + fake
    [ ] COMMIT(b): refactor(ssh_dotnet): decompose Connect() into async pipeline (<=15 complexity)
    [ ] COMMIT(c): refactor(ssh_dotnet): decompose SshTerminalControl + specific exceptions

[ ] >>> Open PR 1 (push feature branch) after Phase 5..4 complete & gates expected green <<<

==============================================================================
 PR 2 — CERTIFICATE LOGIN (follow-up, on PR1's merged base)  (Phases 6-9)
==============================================================================

[ ] PHASE 6 — Cert properties + serialization (XML + CSV only; SQL skipped)
    [ ] AbstractConnectionRecord: add SshDotNetPrivateKeyFile (plaintext) + backing field + AttributeUsedInProtocol(SshDotNet)
    [ ] AbstractConnectionRecord: add SshDotNetPrivateKeyPassphrase (SECRET) + backing field
    [ ] ConnectionInfoInheritance: add bool SshDotNetPrivateKeyFile + SshDotNetPrivateKeyPassphrase (auto-registers via reflection)
    [ ] XmlConnectionNodeSerializer28: write key path (plaintext) ; write passphrase ENCRYPTED (gate on SaveFilter.SavePassword) ; write inherit flags
    [ ] XmlConnectionsDeserializer: read key path (plaintext) ; read passphrase via _decryptor.Decrypt (tolerate failure) ; read inherit flags
    [ ] CSV serializer: add header cols + value cols + inherit cols at MATCHING positions (positional invariant!)
    [ ] CSV deserializer: read matching cols
    [ ] Language.resx (+ Designer): SshDotNetPrivateKeyFile / PropertyDescription... / SshDotNetPrivateKeyPassphrase / PropertyDescription...
    [ ] Tests: XML round-trip auto-covered (keep green) ; ADD CSV column/round-trip + column-count assert
    [ ] Tests: SECURITY (§10.6) ciphertext-at-rest (MANDATORY - round-trip won't catch plaintext leak) ;
        SaveFilter{SavePassword=false}-> "" ; no passphrase in logs
    [ ] Test: load a connections file lacking the new attributes -> defaults cleanly
    [ ] Build + Unit tests green
    [ ] COMMIT: feat(ssh_dotnet): add private-key file/passphrase connection properties (XML+CSV)

[ ] PHASE 7 — Auth wiring + UI
    [ ] SshAuthContext { Username, Password, PrivateKeyPath, Passphrase, AllowKeyboardInteractive }
    [ ] SshAuthenticationProvider.GetAuthenticationMethods(ctx): key (if path) before password; delete null placeholder + TODO
    [ ] BuildPrivateKeyMethod collapses L149 if/else (loader handles empty/non-empty passphrase)
    [ ] Wire BuildAuthentication to read the 2 new props off ConnectionInfo
    [ ] Property-grid: file-picker editor for key path (or plain string v1) ; password/masked editor for passphrase ; localization
    [ ] Error handling: specific messages (FileNotFound / wrong passphrase / malformed key) - no generic catch ; never log secrets
    [ ] Build + Unit tests green
    [ ] COMMIT: feat(ssh_dotnet): wire certificate (private-key) authentication + property-grid UI

[ ] PHASE 8 — Feature tests & manual verification
    [ ] TestKeyFactory (BouncyCastle): ed25519+RSA, encrypted+unencrypted; verify load via PrivateKeyFile
    [ ] Auth matrix Unit tests (§10.3): unencrypted / correct passphrase / wrong passphrase(specific exc) /
        missing file / key+password ordering / key-only / fallback flag
    [ ] [Category("Integration")] opt-in: real/Docker SSH server end-to-end key auth
    [ ] Manual matrix: ed25519 & RSA, enc & unenc, OpenSSH & PEM; happy + each failure; folder-inherited key path
    [ ] Build + Unit tests green
    [ ] COMMIT: test(ssh_dotnet): auth matrix + security tests + runtime key fixtures

[ ] PHASE 9 — Gate dry-run & docs
    [ ] Re-check §7 conventions checklist across touched files
    [ ] Update CHANGELOG.md + IMPLEMENTATION_NOTES.md + connection-property docs
    [ ] Document SQL-backend limitation (XML/CSV only)
    [ ] COMMIT: docs(ssh_dotnet): changelog + cert-login notes + SQL limitation
    [ ] >>> Open PR 2 after this phase <<<

==============================================================================
 PROGRESS LOG (newest first)
==============================================================================
 2026-06-21  CERT FEATURE (PR2) COMPLETE: Phase 6 (48bd7e6c props+serialization+security tests),
             Phase 7 (df26e434 auth wiring key-first+passphrase), Phase 8 (key-auth matrix w/ BouncyCastle TestKeyFactory),
             Phase 9 (88c7af4a changelog). Final SshDotNet Unit: 102/108 (same 6 pre-existing Connect_*). +32 new tests, 0 regressions.
 2026-06-21  Phase 4 partial: 4c Connect decomposition (e60338b9), 4f FormatBytes/terminal statics+dead fields (108b95a5,7adcba14),
             4e narrowed fatal-path catches (3ca395ed). PortForwardRuleParser split+15 tests. Dispose/CreateAdapter tests.
             REMAINING (deferred, non-gate-blocking): 4d SshTerminalControl decomposition, 4e remaining non-fatal/IO-loop catches.
 2026-06-21  Phase 4b committed: ISshClientAdapter seam (protocol depends on interface; native ConnectAsync ready for 4c).
 2026-06-21  Phase 4a committed (08b37384,1fba3398): IDisposable+CTS+ProtocolBase.Dispose fix. ALL reliability bugs fixed => QG likely green.
 2026-06-21  Phase 3 renames DONE: 3b+3c (enum+namespace+folder) committed; 3d (6231d4a3) serialized prop. 3e folded into Phase 4.
 2026-06-21  Phase 3a committed (9c3be904): 5 SSH_DotNet classes+files renamed to PascalCase (S101).
 2026-06-21  NOTE: plan .md is tracked (committed earlier in 56188a51 'plan implemented'); ToDo .txt stays untracked.
 2026-06-21  Phase 2 committed (8cf5980b): deleted reflection pty-resize, used public ChangeWindowSize. Hotspot removed at source.
 2026-06-21  Phase 1 committed (268520fb): monotonic timing. IDisposable/CTS-dispose moved to Phase 4 (race-safe ordering).
 2026-06-21  Phase 5 committed (test dedup + [Category] taxonomy). 6 pre-existing Connect_* failures deferred to Phase 4.
 2026-06-21  Phase 0 done: baseline build green; 2295/2192/103 test baseline recorded.
 2026-06-21  ToDo created; starting Phase 0 baseline.
